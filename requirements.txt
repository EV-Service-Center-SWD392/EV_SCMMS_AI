# Dependencies for EV SCMMS Chatbot
requests>=2.28.0
python-dateutil>=2.8.0
flask>=2.0.0  # Optional: if using Flask integration
fastapi>=0.68.0  # Optional: if using FastAPI integration
uvicorn>=0.15.0  # Optional: if using FastAPI integration